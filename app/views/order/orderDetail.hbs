<header class="ui-header ui-header-positive ui-border-b">
    <i class="ui-icon-return" onclick="history.back()"></i>
    <h1>订单详情</h1>
</header>
<div class="ui-container ui-whitespace">
    订单号: {{order.id}}

</div>
<div class="ui-container ui-whitespace">
    <table class="ui-table ui-border-tb">
        <thead>
            <tr>
                <th>产品名称</th>
                <th>类型</th>
                <th>数量</th>
                <th>地址</th>
                <th>状态</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{order.productName}}</td>
                <td>{{order.prodcutCate}}</td>
                <td>{{order.productNum}}</td>
                <td>{{order.address}}</td>
                <td>{{order.status}}</td>
            </tr>
        </tbody>
    </table>
</div>

<div class="ui-container ui-whitespace">
    备注： {{order.remark}} {{!-- {{order}} --}}
</div>
<a href="/">
    <div class="ui-btn-wrap">
        <button class="ui-btn-lg">
            返回首页
        </button>
    </div>
</a>